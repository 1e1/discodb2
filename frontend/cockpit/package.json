{
  "name": "discodb2-cockpit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "discodb2 COCKPIT — heavy CAN reverse-engineering frontend (buffer, decode, analysis, DBC, export, Wizard host).",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^4.2.19",
    "svelte-check": "^3.8.6",
    "tslib": "^2.7.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "expr-eval": "^2.0.2"
  }
}
